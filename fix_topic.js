const fs = require('fs'); let d = fs.readFileSync('videos.html', 'utf8'); if (!d.includes('.topic-btn {')) { d = d.replace('.filter-row {', '.topic-btn { display: inline-flex; align-items: center; gap: 6px; padding: 7px 14px; border-radius: var(--radius-full); font-size: 13px; font-weight: 500; color: var(--text-secondary); background: transparent; border: 1.5px solid var(--border); transition: var(--transition); cursor: pointer; } .topic-btn:hover { border-color: var(--primary); color: var(--primary); background: var(--primary-light); } .topic-btn.active { border-color: var(--primary); background: var(--primary-light); color: var(--primary-dark); font-weight: 600; }\n        .filter-row {'); fs.writeFileSync('videos.html', d); console.log('patched topic-btn'); }
